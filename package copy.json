{
  "name": "commands-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ltyu/commands-subgraph",
    "create-local": "graph create --node http://localhost:8020/ ltyu/commands-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ ltyu/commands-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ltyu/commands-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
